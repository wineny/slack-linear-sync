# 🎫 Slack 이모지 → Linear 이슈 자동 생성 기능

> **프로젝트명**: Emoji Issue Creator  
> **작성일**: 2026-01-22  
> **최종 수정**: 2026-01-22  
> **상태**: ✅ Phase 4 완료 - 기본 동작 확인됨 (프로젝트 매칭 개선 필요)

---

## 📋 목차

1. [개요](#1-개요)
2. [기존 기능과의 차이점](#2-기존-기능과의-차이점)
3. [아키텍처](#3-아키텍처)
4. [파일 구조](#4-파일-구조)
5. [상세 구현 계획](#5-상세-구현-계획)
6. [API 스펙](#6-api-스펙)
7. [프롬프트 공유 전략](#7-프롬프트-공유-전략)
8. [Slack App 설정](#8-slack-app-설정)
9. [배포 계획](#9-배포-계획)
10. [테스트 시나리오](#10-테스트-시나리오)

---

## 1. 개요

### 1.1 배경

Slack에서 대화 중 이슈로 만들어야 할 내용이 있을 때, 기존 Linear 공식 앱은:
- 메뉴 클릭이 3번 이상 필요 (번거로움)
- AI 요약 기능 없음 (맥락 정리가 안 됨)
- 스레드 전체 맥락을 파악하지 못함

### 1.2 목표

**이모지 한 번 클릭으로 AI가 대화 맥락을 분석하여 Linear 이슈 자동 생성**

### 1.3 핵심 기능

| 기능 | 설명 |
|------|------|
| 트리거 | `:이슈:` 커스텀 이모지 클릭 |
| 범위 | 봇이 초대된 모든 채널 |
| AI 분석 | Claude Haiku로 제목/설명/프로젝트 자동 추천 |
| 팀 배정 | 추천된 프로젝트에 따라 자동 결정 |
| Assignee | 이모지 누른 사람 (기본값) |
| 중복 허용 | 같은 메시지에 여러 이슈 생성 가능 |
| 알림 | 스레드 답글로 이슈 링크 전송 |

---

## 2. 기존 기능과의 차이점

### ⚠️ 중요: 두 기능은 완전히 분리되어 있음

이 프로젝트에는 **두 가지 독립적인 기능**이 있습니다:

### 2.1 뽀시래기 (Pposiraegi) - 기존 기능

| 항목 | 내용 |
|------|------|
| **위치** | `src/handlers/pposiraegi/question-handler.ts` ✅ 분리 완료 |
| **트리거** | 특정 채널(`00-ai개발-질문`)에 메시지 작성 |
| **동작** | 메시지 작성 = 자동 이슈 생성 |
| **Assignee** | 메시지에서 멘션된 사람 |
| **팀** | 고정 (`LINEAR_TEAM_ID`) |
| **완료 처리** | `:해결:` 이모지로 Done 처리 (`pposiraegi/done-handler.ts`) |

### 2.2 Emoji Issue Creator - 신규 기능

| 항목 | 내용 |
|------|------|
| **위치** | `src/handlers/emoji-issue/` (새 폴더) |
| **트리거** | `:이슈:` 이모지 클릭 |
| **동작** | 이모지 클릭 시에만 이슈 생성 |
| **Assignee** | 이모지 누른 사람 |
| **팀** | AI가 추천한 프로젝트에 따라 자동 결정 |
| **범위** | 모든 채널 |

### 2.3 코드 분리 원칙 ✅ 구현 완료

```
src/handlers/
├── slack-events.ts           # 라우터 (54줄) ✅
├── slack-reactions.ts        # 리액션 라우터 (24줄) ✅
│
├── pposiraegi/               # 🐣 뽀시래기 전용 폴더 ✅
│   ├── index.ts              # export 모음
│   ├── question-handler.ts   # 질문 처리 로직 (228줄)
│   └── done-handler.ts       # 완료 처리 (85줄)
│
└── emoji-issue/              # 🎫 Emoji Issue Creator 전용 폴더 ✅
    ├── index.ts              # export 모음
    ├── handler.ts            # 메인 핸들러 (115줄)
    └── thread-collector.ts   # 스레드 메시지 수집 (53줄)
```

---

## 3. 아키텍처

### 3.1 전체 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Slack 채널 (어디서든)                                                    │
│                                                                          │
│  [대화 스레드]                                                            │
│    ├── 김팀장: API 응답이 느려요                                          │
│    ├── 박개발: 캐싱 문제 같아요                                           │
│    └── 이기획: DB 쿼리도 최적화 필요할 듯                                  │
│                                                                          │
│        김팀장이 :이슈: 이모지 추가                                         │
│                    │                                                     │
│                    ▼                                                     │
│  [봇 답글]                                                                │
│  ✅ Linear 이슈가 생성되었습니다!                                         │
│  📋 GPT-142: [API] 응답 속도 개선 필요                                   │
│  📁 프로젝트: Backend Optimization                                       │
│  👤 담당자: 김팀장                                                        │
│  👉 Linear에서 확인하기                                                   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  slack-linear-sync (Cloudflare Worker)                                   │
│                                                                          │
│  1. reaction_added 이벤트 수신                                            │
│  2. 이모지가 "이슈"인지 확인                                               │
│  3. 메시지/스레드 내용 수집                                                │
│  4. linear-capture-worker로 AI 분석 요청                                  │
│                    │                                                     │
└────────────────────│─────────────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  linear-capture-worker (Cloudflare Worker) - AI 분석 중앙화              │
│                                                                          │
│  POST /analyze-text                                                      │
│  - 공유 프롬프트로 분석                                                    │
│  - 프로젝트 추천                                                          │
│  - 제목/설명 생성                                                         │
│                    │                                                     │
└────────────────────│─────────────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  slack-linear-sync (계속)                                                │
│                                                                          │
│  5. 프로젝트 → 팀 매핑                                                    │
│  6. Assignee 매핑 (이모지 누른 사람)                                      │
│  7. Linear 이슈 생성                                                      │
│  8. Slack 스레드에 알림                                                   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2 시퀀스 다이어그램

```
User          Slack           slack-linear-sync    linear-capture-worker    Linear API
  │             │                    │                      │                   │
  │──:이슈:────▶│                    │                      │                   │
  │             │──reaction_added───▶│                      │                   │
  │             │                    │                      │                   │
  │             │                    │──get thread msgs────▶│                   │
  │             │◀───messages────────│                      │                   │
  │             │                    │                      │                   │
  │             │                    │──POST /analyze-text─▶│                   │
  │             │                    │◀──analysis result────│                   │
  │             │                    │                      │                   │
  │             │                    │──────────────────────────createIssue────▶│
  │             │                    │◀─────────────────────────issue result────│
  │             │                    │                      │                   │
  │             │◀──thread reply─────│                      │                   │
  │◀────────────│                    │                      │                   │
```

---

## 4. 파일 구조

### 4.1 slack-linear-sync 변경 후 구조 ✅ 구현 완료

```
slack-linear-sync/
├── src/
│   ├── index.ts                      # Hono 라우터 (변경 없음)
│   │
│   ├── handlers/
│   │   ├── slack-events.ts           # 이벤트 라우터 ✅
│   │   ├── slack-reactions.ts        # 리액션 라우터 (이슈/해결 분기) ✅
│   │   │
│   │   ├── pposiraegi/               # 🐣 뽀시래기 ✅
│   │   │   ├── index.ts              # export 모음
│   │   │   ├── question-handler.ts   # 질문 자동 이슈 생성
│   │   │   └── done-handler.ts       # :해결: 이모지 → Done 처리
│   │   │
│   │   └── emoji-issue/              # 🎫 Emoji Issue Creator ✅
│   │       ├── index.ts              # export 모음
│   │       ├── handler.ts            # 메인 핸들러
│   │       └── thread-collector.ts   # 스레드 메시지 수집
│   │
│   ├── services/
│   │   ├── slack-client.ts           # ✅ getThreadMessages, getMessage 추가
│   │   ├── linear-client.ts          # ✅ getProjects 추가
│   │   ├── ai-analyzer.ts            # 기존 (뽀시래기용)
│   │   └── ai-worker-client.ts       # ✅ 신규: Worker API 호출
│   │
│   ├── types/
│   │   └── index.ts                  # ✅ ISSUE_EMOJI, AI_WORKER_URL, LinearProject 추가
│   │
│   └── utils/
│       ├── signature.ts              # 변경 없음
│       ├── token-manager.ts          # 변경 없음
│       └── user-mapper.ts            # 변경 없음
│
├── wrangler.toml                     # ✅ ISSUE_EMOJI, AI_WORKER_URL 추가
├── package.json
├── tsconfig.json
├── README.md
├── PLAN_EMOJI_ISSUE_CREATOR.md       # 이 문서
└── DEVLOG.md
```
slack-linear-sync/
├── src/
│   ├── index.ts                      # Hono 라우터 (변경 없음)
│   │
│   ├── handlers/
│   │   ├── slack-events.ts           # 이벤트 라우터 (변경 최소화)
│   │   ├── slack-reactions.ts        # 리액션 라우터 (분기 추가)
│   │   │
│   │   ├── pposiraegi/               # 🐣 뽀시래기 (기존 기능 이동)
│   │   │   ├── index.ts              # export 모음
│   │   │   ├── question-handler.ts   # 질문 자동 이슈 생성
│   │   │   └── done-handler.ts       # :해결: 이모지 → Done 처리
│   │   │
│   │   └── emoji-issue/              # 🎫 Emoji Issue Creator (신규)
│   │       ├── index.ts              # export 모음
│   │       ├── handler.ts            # 메인 핸들러
│   │       ├── thread-collector.ts   # 스레드 메시지 수집
│   │       └── constants.ts          # 상수 정의
│   │
│   ├── services/
│   │   ├── slack-client.ts           # 🔧 수정: getThreadMessages 추가
│   │   ├── linear-client.ts          # 🔧 수정: getProjects 추가
│   │   ├── ai-analyzer.ts            # 기존 (뽀시래기용)
│   │   └── ai-worker-client.ts       # 🆕 신규: Worker API 호출
│   │
│   ├── types/
│   │   ├── index.ts                  # 공통 타입
│   │   ├── pposiraegi.ts             # 🆕 뽀시래기 전용 타입
│   │   └── emoji-issue.ts            # 🆕 Emoji Issue Creator 전용 타입
│   │
│   └── utils/
│       ├── signature.ts              # 변경 없음
│       ├── token-manager.ts          # 변경 없음
│       └── user-mapper.ts            # 변경 없음
│
├── wrangler.toml                     # 🔧 수정: ISSUE_EMOJI 추가
├── package.json
├── tsconfig.json
├── README.md
├── PLAN_EMOJI_ISSUE_CREATOR.md       # 이 문서
└── DEVLOG.md
```

### 4.2 linear-capture-worker 변경 후 구조 ✅ 구현 완료

```
linear-capture-worker/
├── src/
│   ├── index.ts                      # ✅ /analyze-text 엔드포인트 추가
│   │
│   └── prompts/                      # ✅ 공유 프롬프트 모듈
│       ├── index.ts                  # export 모음
│       ├── issue-prompt.ts           # 이슈 생성 프롬프트 (이미지/텍스트 지원)
│       └── types.ts                  # 프롬프트 관련 타입
│
├── wrangler.toml
├── package.json
└── tsconfig.json

✅ 배포 완료: https://linear-capture-ai.ny-4f1.workers.dev
✅ /analyze-text 테스트 완료
```

---

## 5. 상세 구현 계획

### 5.1 Phase 1: 파일 구조 리팩토링 ✅ 완료

**목표**: 뽀시래기 코드를 별도 폴더로 분리

#### 5.1.1 `src/handlers/pposiraegi/question-handler.ts` ✅
- `slack-events.ts`의 `processQuestion()` 함수 이동
- 중복체크 로직 (`isDuplicate`, `processedMessages`) 포함
- 기존 동작 100% 유지 ✅ 회귀 테스트 통과

#### 5.1.2 `src/handlers/pposiraegi/done-handler.ts` ✅
- `slack-reactions.ts`의 Done 처리 로직 이동
- `:해결:` 이모지 처리

#### 5.1.3 `src/handlers/slack-events.ts` 수정 ✅
- 라우터 역할만 수행 (279줄 → 54줄)
- `handleQuestion` import 추가

---

### 5.2 Phase 2: linear-capture-worker 수정 ✅ 완료

**목표**: 텍스트 분석 엔드포인트 추가 및 프롬프트 모듈화

#### 5.2.1 `src/prompts/` 폴더 구조 ✅
```
src/prompts/
├── index.ts          # export 모음
├── types.ts          # PromptContext, PromptInput, TextAnalysisRequest 등
└── issue-prompt.ts   # buildImagePrompt(), buildTextPrompt(), buildIssuePrompt()
```

#### 5.2.2 `src/index.ts` - 새 엔드포인트 ✅
- `/analyze-text` 엔드포인트 추가
- 기존 이미지 분석에서 `buildImagePrompt()` 사용하도록 리팩토링

#### 5.2.3 배포 및 테스트 ✅
```bash
# 배포 완료
https://linear-capture-ai.ny-4f1.workers.dev

# 테스트 결과
curl -X POST .../analyze-text -d '{"messages":[...]}'
→ {"title":"API 응답 성능 개선 (캐싱 & DB 쿼리 최적화)","success":true,...}
```

---

### 5.3 Phase 3: Emoji Issue Creator 구현 ✅ 완료

#### 5.3.1 `src/services/slack-client.ts` ✅
- `getThreadMessages(channel, threadTs)` 추가 - 스레드 메시지 최대 100개 조회
- `getMessage(channel, ts)` 추가 - 단일 메시지 조회

#### 5.3.2 `src/services/linear-client.ts` ✅
- `getProjects()` 추가 - 활성 프로젝트 목록 조회 (state: "started")
- `LinearProject` 타입 추가 (types/index.ts)

#### 5.3.3 `src/services/ai-worker-client.ts` ✅
- `analyzeText(workerUrl, request)` 함수 생성
- Worker URL을 환경변수에서 받도록 구현

#### 5.3.4 `src/handlers/emoji-issue/` ✅
```
emoji-issue/
├── index.ts              # export
├── handler.ts            # handleEmojiIssue() - 메인 핸들러
└── thread-collector.ts   # collectThreadMessages() - 스레드 수집
```

**handler.ts 구현 내용:**
1. 리액션된 메시지 조회
2. 스레드 여부 확인 & 메시지 수집
3. Linear 프로젝트/사용자 목록 조회 (병렬)
4. Worker로 AI 분석 요청
5. 프로젝트 → 팀 매핑 (suggestedProjectId → teamId)
6. Assignee 매핑 (이모지 누른 사람)
7. Linear 이슈 생성
8. Slack 스레드에 알림

#### 5.3.5 `src/handlers/slack-reactions.ts` ✅
```typescript
if (event.reaction === env.ISSUE_EMOJI) {
  await handleEmojiIssue(event, env);
}
if (event.reaction === env.DONE_EMOJI) {
  await handleDone(event, env);
}
```

---

### 5.4 Phase 4: 환경변수 및 설정 🔄 진행 중

#### 5.4.1 `wrangler.toml` ✅ 완료
```toml
[vars]
# 기존 (뽀시래기)
TARGET_CHANNEL_NAME = "00-ai개발-질문"
LINEAR_TEAM_ID = "e108ae14-a354-4c09-86ac-6c1186bc6132"
LINEAR_DEFAULT_STATE_ID = "6dc4154e-3a35-43d2-ac44-e3d66df85c9b"
LINEAR_DONE_STATE_ID = "8af3af6f-d60f-4d57-bac2-fcd557488d93"
DONE_EMOJI = "해결"

# 🆕 Emoji Issue Creator ✅ 추가됨
ISSUE_EMOJI = "이슈"
AI_WORKER_URL = "https://linear-capture-ai.ny-4f1.workers.dev"
```

#### 5.4.2 남은 작업
- [ ] `wrangler deploy` (slack-linear-sync)
- [ ] Slack App에서 `reaction_added` 이벤트 구독 확인
- [ ] `:이슈:` 커스텀 이모지 생성 (Slack 워크스페이스)
- [ ] 실제 테스트

---

## 6. API 스펙

### 6.1 linear-capture-worker: POST /analyze-text

#### Request
```typescript
interface TextAnalysisRequest {
  messages: Array<{
    author: string;    // 작성자 이름
    text: string;      // 메시지 내용
  }>;
  context?: {
    projects: Array<{
      id: string;
      name: string;
      description?: string;
    }>;
    users: Array<{
      id: string;
      name: string;
    }>;
  };
  slackPermalink?: string;  // Slack 원본 링크
}
```

#### Response
```typescript
interface TextAnalysisResult {
  title: string;              // 이슈 제목
  description: string;        // 이슈 설명 (마크다운)
  success: boolean;
  suggestedProjectId?: string;  // 추천 프로젝트 ID
  suggestedPriority?: number;   // 추천 우선순위 (1-4)
  error?: string;
}
```

#### Example
```bash
curl -X POST https://linear-capture-ai.ny-4f1.workers.dev/analyze-text \
  -H "Content-Type: application/json" \
  -d '{
    "messages": [
      {"author": "김팀장", "text": "API 응답이 3초나 걸려요"},
      {"author": "박개발", "text": "캐싱이 안 되어 있는 것 같아요"},
      {"author": "이기획", "text": "DB 쿼리 최적화도 필요해 보여요"}
    ],
    "context": {
      "projects": [
        {"id": "proj-1", "name": "Backend Optimization", "description": "백엔드 성능 개선"},
        {"id": "proj-2", "name": "Frontend", "description": "프론트엔드 개발"}
      ]
    },
    "slackPermalink": "https://gpters.slack.com/archives/C123/p456"
  }'
```

---

## 7. 프롬프트 공유 전략

### 7.1 공유 프롬프트 위치

```
linear-capture-worker/src/prompts/issue-prompt.ts
```

### 7.2 사용처

| 프로젝트 | 엔드포인트 | 입력 타입 |
|---------|-----------|----------|
| linear-capture (Desktop) | POST / | 이미지 (스크린샷) |
| slack-linear-sync | POST /analyze-text | 텍스트 (Slack 메시지) |

### 7.3 프롬프트 수정 시 반영 방법

1. `linear-capture-worker/src/prompts/issue-prompt.ts` 수정
2. Worker 배포: `wrangler deploy`
3. **끝!** (모든 클라이언트에 자동 반영)

### 7.4 프롬프트 구조

```typescript
// 공통 섹션
const TITLE_RULES = `...`;        // 제목 규칙
const DESCRIPTION_TEMPLATE = `...`; // 설명 템플릿
const PROJECT_MATCHING = `...`;    // 프로젝트 매칭 규칙

// 입력 타입별 분기
function buildPrompt(input: PromptInput, context?: PromptContext): string {
  if (input.type === 'image') {
    return buildImagePrompt(input.imageCount, context);
  } else {
    return buildTextPrompt(input.messages, context);
  }
}
```

---

## 8. Slack App 설정

### 8.1 필요한 Event Subscriptions

```
reaction_added    ← 이미 있을 수 있음 (뽀시래기 Done 처리용)
```

### 8.2 필요한 Bot Token Scopes

```
reactions:read       ← 이미 있을 수 있음
channels:history     ← 이미 있음
channels:read        ← 이미 있음
chat:write           ← 이미 있음
users:read           ← 이미 있음
users:read.email     ← 이미 있음
groups:history       ← 🆕 private 채널 지원 시 필요
```

### 8.3 커스텀 이모지 생성

Slack 워크스페이스에서 `:이슈:` 커스텀 이모지 생성 필요

---

## 9. 배포 계획

### 9.1 배포 순서

```
1. ✅ linear-capture-worker 배포 (프롬프트 모듈화 + /analyze-text)
   └── 완료: https://linear-capture-ai.ny-4f1.workers.dev

2. 🔄 slack-linear-sync 배포 (Emoji Issue Creator)
   └── cd slack-linear-sync && wrangler deploy

3. ⏳ Slack App 설정 확인
   └── Event Subscriptions에 reaction_added 있는지 확인

4. ⏳ 커스텀 이모지 생성
   └── Slack 워크스페이스에서 :이슈: 이모지 추가
```

### 9.2 배포 명령어

```bash
# slack-linear-sync 배포
cd /Users/wine_ny/side-project/linear_project/slack-linear-sync
wrangler deploy
```

### 9.3 롤백 계획

문제 발생 시:
1. `wrangler.toml`에서 `ISSUE_EMOJI` 주석 처리
2. 재배포

---

## 10. 테스트 시나리오

### 10.1 기본 테스트

| # | 시나리오 | 예상 결과 |
|---|---------|----------|
| 1 | 단일 메시지에 :이슈: 이모지 | 해당 메시지로 이슈 생성 |
| 2 | 스레드 부모 메시지에 :이슈: | 스레드 전체 내용으로 이슈 생성 |
| 3 | 스레드 답글에 :이슈: | 스레드 전체 내용으로 이슈 생성 |
| 4 | 같은 메시지에 2명이 :이슈: | 2개의 이슈 생성 (각자 Assignee) |

### 10.2 엣지 케이스

| # | 시나리오 | 예상 결과 |
|---|---------|----------|
| 5 | 봇 메시지에 :이슈: | 무시 (처리 안 함) |
| 6 | 이미지만 있는 메시지에 :이슈: | 이미지 캡션 또는 기본 제목으로 생성 |
| 7 | :이슈: 이모지 제거 | 아무 동작 없음 |
| 8 | 프로젝트 매칭 실패 | 기본 팀(LINEAR_TEAM_ID)으로 생성 |

### 10.3 뽀시래기 회귀 테스트

| # | 시나리오 | 예상 결과 |
|---|---------|----------|
| 9 | 00-ai개발-질문에 메시지 작성 | 기존처럼 자동 이슈 생성 |
| 10 | 이슈에 :해결: 이모지 | 기존처럼 Done 처리 |

---

## 📝 체크리스트

### Phase 1: 파일 구조 리팩토링 ✅ 완료
- [x] `src/handlers/pposiraegi/` 폴더 생성
- [x] `question-handler.ts` 분리
- [x] `done-handler.ts` 분리
- [x] `slack-events.ts` 수정 (라우터 역할만)
- [x] 뽀시래기 회귀 테스트 ✅ 통과

### Phase 2: linear-capture-worker 수정 ✅ 완료
- [x] `src/prompts/` 폴더 생성
- [x] `prompts/types.ts` 생성
- [x] `prompts/issue-prompt.ts` 모듈화
- [x] `/analyze-text` 엔드포인트 추가
- [x] Worker 배포 ✅ 완료
- [x] `/analyze-text` 테스트 ✅ 정상 동작

### Phase 3: Emoji Issue Creator 구현 ✅ 완료
- [x] `src/handlers/emoji-issue/` 폴더 생성
- [x] `slack-client.ts`에 `getThreadMessages()`, `getMessage()` 추가
- [x] `linear-client.ts`에 `getProjects()` 추가
- [x] `ai-worker-client.ts` 신규 생성
- [x] `emoji-issue/handler.ts` 구현
- [x] `emoji-issue/thread-collector.ts` 구현
- [x] `slack-reactions.ts` 분기 추가
- [x] `types/index.ts`에 `ISSUE_EMOJI`, `AI_WORKER_URL`, `LinearProject` 추가
- [x] `wrangler.toml`에 환경변수 추가
- [x] TypeScript 빌드 검증 ✅ 통과

### Phase 4: 배포 및 테스트 ✅ 완료
- [x] 환경변수 추가 (`ISSUE_EMOJI`, `AI_WORKER_URL`)
- [x] slack-linear-sync Worker 배포 ✅
- [x] Slack App 설정 확인 (`reaction_added` 이벤트) ✅
- [x] `:issue:` 이모지로 테스트 (`:이슈:` 대신)
- [x] 봇을 채널에 초대해야 `reaction_added` 이벤트 수신됨 ✅ 확인
- [x] 이벤트 수신 → 메시지 수집 → AI Worker 호출 단계까지 도달 ✅
- [x] **Worker 간 통신 404 문제 발생 → 직접 Anthropic API 호출로 해결** ✅
- [x] 이슈 생성 동작 확인 ✅
- [ ] 프로젝트 자동 매칭 개선 필요 (현재 기본 팀으로 생성됨)
- [ ] 문서 업데이트 (CLAUDE.md, README.md)

---

## 🚨 트러블슈팅 로그

### 문제 1: `reaction_added` 이벤트가 Worker로 전송되지 않음

**증상**: `:issue:` 이모지를 눌러도 Worker 로그에 아무것도 안 뜸

**원인**: 봇이 해당 채널에 초대되어 있지 않았음

**해결**: `/invite @뽀시래기`로 채널에 봇 초대 → 이벤트 정상 수신

---

### 문제 2: AI 분석 실패 - Worker 간 통신 404 에러 (현재 진행 중)

**증상**: 
```
이슈 생성 실패: Worker API error: 404 - error code: 1042
```

**상세 분석**:
- `slack-linear-sync` Worker에서 `linear-capture-worker`의 `/analyze-text` 엔드포인트 호출 시 404 발생
- **로컬 curl 테스트는 정상 동작**:
  ```bash
  curl -X POST https://linear-capture-ai.ny-4f1.workers.dev/analyze-text \
    -H "Content-Type: application/json" \
    -d '{"messages":[{"author":"test","text":"API 응답이 느려요"}]}'
  # → 정상 응답: {"title":"...","success":true}
  ```
- **Worker 간 fetch 호출 시에만 404 발생**

**Error Code 1042 의미**: Cloudflare Worker가 대상 Worker를 찾을 수 없음

**가능한 원인**:
1. Cloudflare의 같은 계정 내 Worker 간 통신 제한
2. Worker Subdomain 라우팅 문제
3. DNS 전파 지연

**시도한 해결책**:
- [x] `AI_WORKER_URL` 환경변수 값 확인 → 정상
- [x] User-Agent 헤더 추가 → 효과 없음
- [x] 상세 로깅 추가 → 404 확인

**다음 시도할 해결책**:

#### 옵션 1: 직접 Anthropic API 호출 (권장) ⭐

Worker 간 통신을 포기하고, `slack-linear-sync`에서 직접 Claude API 호출

```
변경 전:
slack-linear-sync → linear-capture-worker → Anthropic API

변경 후:
slack-linear-sync → Anthropic API (직접)
```

**구현 계획**:
1. `src/services/ai-analyzer.ts` 수정 - 텍스트 분석 함수 추가
2. `src/handlers/emoji-issue/handler.ts` 수정 - `ai-worker-client` 대신 `ai-analyzer` 사용
3. `ai-worker-client.ts` 제거 또는 비활성화

**장점**:
- 즉시 해결 가능 (기존 `ai-analyzer.ts` 활용)
- Worker 간 통신 문제 완전 회피
- 지연 시간 감소 (한 단계 줄어듦)

**단점**:
- 프롬프트가 두 곳에 존재 (`linear-capture-worker`, `slack-linear-sync`)
- 프롬프트 수정 시 두 곳 모두 업데이트 필요

**프롬프트 중복 해결 방안**:
- 핵심 프롬프트를 npm 패키지로 분리 (나중에)
- 또는 프롬프트를 환경변수/KV로 관리 (나중에)
- **당장은 중복 허용** - 동작하는 게 우선

---

#### 옵션 2: Service Binding 사용

Cloudflare의 [Service Bindings](https://developers.cloudflare.com/workers/runtime-apis/bindings/service-bindings/) 활용

```toml
# slack-linear-sync/wrangler.toml
[[services]]
binding = "AI_WORKER"
service = "linear-capture-worker"
```

```typescript
// 사용법
const response = await env.AI_WORKER.fetch(request);
```

**장점**:
- 프롬프트 중앙화 유지
- 네트워크 홉 없이 직접 호출

**단점**:
- 설정 복잡
- 두 Worker가 같은 Cloudflare 계정에 있어야 함

---

#### 옵션 3: Custom Domain 사용

`linear-capture-worker`에 커스텀 도메인 연결

**장점**: 표준 HTTP 호출로 문제 회피 가능성
**단점**: 도메인 설정 필요, 근본 해결인지 불확실

---

### 결정: 옵션 1 (직접 Anthropic API 호출) 선택 ✅ 구현 완료

**이유**:
1. 가장 빠르게 구현 가능
2. 기존 코드 재활용 (`ai-analyzer.ts`)
3. 외부 의존성 제거 (Worker 간 통신 불안정성 회피)
4. 프롬프트 중복은 나중에 리팩토링으로 해결

**구현 내용** (2026-01-22):
1. `ai-analyzer.ts`에 `analyzeSlackThread()` 메서드 추가
2. `linear-capture-worker`와 동일한 프롬프트 사용 (TITLE_RULES, DESCRIPTION_TEMPLATE)
3. `handler.ts`에서 `ai-worker-client` 대신 `AIAnalyzer` 직접 사용
4. 배포 및 테스트 완료 → **이슈 생성 동작 확인** ✅

---

### 문제 3: 프로젝트 자동 매칭이 제대로 안 됨 (개선 필요)

**증상**: 이슈는 생성되지만 프로젝트가 "기본"으로 표시됨

**가능한 원인**:
1. AI가 `projectId`를 `null`로 반환
2. AI가 반환한 ID가 실제 프로젝트 목록에 없음
3. 프롬프트에서 프로젝트 매칭 지시가 불충분

**다음 단계**:
- 로그에서 AI 응답의 `suggestedProjectId` 값 확인 필요
- 프롬프트 개선 또는 프로젝트 매칭 로직 수정

---

## 🔗 관련 문서

- [linear-capture README](../linear-capture/README.md)
- [linear-capture-worker](../linear-capture-worker/)
- [Linear GraphQL API](https://developers.linear.app/docs/graphql/working-with-the-graphql-api)
- [Slack Events API](https://api.slack.com/events)

---

## 📅 작업 이력

| 날짜 | 작업 내용 | 상태 |
|------|----------|------|
| 2026-01-22 | Phase 1~3 구현 완료 | ✅ |
| 2026-01-22 | Phase 4 배포 시작, 봇 초대 문제 해결 | ✅ |
| 2026-01-22 | Worker 간 통신 404 에러 발생 | ✅ 해결 |
| 2026-01-22 | 직접 Anthropic API 호출로 변경, 이슈 생성 동작 확인 | ✅ |
| 2026-01-22 | 프로젝트 자동 매칭 개선 필요 발견 | 🚧 |
