# /initiative-update 슬래시 커맨드 설정 가이드

## 개요

`/initiative-update` 커맨드는 이니셔티브 리드가 소속 프로젝트들의 주간 업데이트를 통합 조회하는 기능입니다.

**배포 상태**: ✅ 완료 (https://slack-linear-sync.ny-4f1.workers.dev)  
**설정 필요**: Slack App에 커맨드 등록

---

## 1. Slack App 커맨드 등록

### 1.1 Slack App 관리 페이지 접속

1. https://api.slack.com/apps 접속
2. 앱 선택 (예: `Linear Issue Creator`)

### 1.2 Slash Command 추가

1. 왼쪽 메뉴에서 **Slash Commands** 클릭
2. **Create New Command** 버튼 클릭
3. 다음 정보 입력:

| 필드 | 값 |
|------|-----|
| **Command** | `/initiative-update` |
| **Request URL** | `https://slack-linear-sync.ny-4f1.workers.dev/slack/command` |
| **Short Description** | `이니셔티브 통합 업데이트 조회` |
| **Usage Hint** | (비워두기) |

4. **Save** 버튼 클릭

### 1.3 앱 재설치

**중요**: 새 커맨드를 추가한 후에는 반드시 앱을 재설치해야 합니다.

1. 왼쪽 메뉴에서 **Install App** 클릭
2. **Reinstall to Workspace** 버튼 클릭
3. 권한 승인

---

## 2. 테스트

### 2.1 기본 테스트

1. Slack 워크스페이스에서 아무 채널이나 선택
2. 메시지 입력창에 `/initiative-update` 입력
3. Enter 키 입력

### 2.2 예상 동작

**즉시 응답** (ephemeral 메시지):
```
📋 이니셔티브 업데이트를 가져오는 중...
```

**최종 응답** (몇 초 후):
```
📋 *{Initiative 이름}*

📊 *{Project 1 이름}*
{Update 본문 내용}

📊 *{Project 2 이름}*
업데이트 없음 - <작성하기>

---

📋 *{다른 Initiative 이름}*
...
```

### 2.3 테스트 시나리오

| 시나리오 | 예상 결과 |
|---------|----------|
| 리드인 이니셔티브가 있는 사용자 | 이니셔티브별 프로젝트 업데이트 표시 |
| 리드인 이니셔티브가 없는 사용자 | `❌ 리드하는 이니셔티브가 없습니다.` |
| Linear 계정이 없는 사용자 | `❌ Linear 계정을 찾을 수 없습니다.` |
| 프로젝트에 업데이트가 없는 경우 | `업데이트 없음 - <작성하기>` 링크 표시 |

---

## 3. 문제 해결

### 3.1 커맨드가 나타나지 않음

**원인**: 앱 재설치를 하지 않음

**해결**:
1. https://api.slack.com/apps → 앱 선택
2. **Install App** → **Reinstall to Workspace**

### 3.2 "dispatch_failed" 오류

**원인**: Request URL이 잘못됨

**해결**:
1. Slash Commands 설정 확인
2. Request URL이 정확한지 확인: `https://slack-linear-sync.ny-4f1.workers.dev/slack/command`

### 3.3 "Linear 계정을 찾을 수 없습니다" 오류

**원인**: Slack 이메일과 Linear 이메일이 일치하지 않음

**해결**:
1. Slack 프로필에서 이메일 확인
2. Linear 계정 이메일과 일치하는지 확인
3. 필요시 Linear 계정 이메일 변경

### 3.4 응답이 없음

**원인**: Worker가 다운되었거나 네트워크 문제

**해결**:
1. Worker 상태 확인: https://slack-linear-sync.ny-4f1.workers.dev/
2. 예상 응답: `{"status":"ok","service":"slack-linear-sync"}`
3. 응답이 없으면 Cloudflare Workers 대시보드 확인

---

## 4. 기술 세부사항

### 4.1 데이터 소스

- **Initiative**: Linear API에서 본인이 owner인 initiatives 조회
- **Projects**: 각 initiative의 연결된 projects 조회
- **Updates**: 각 project의 이번 주(월요일 00:00 KST ~) 작성된 projectUpdates 조회

### 4.2 시간 범위

- **기준**: 이번 주 월요일 00:00 KST
- **타임존**: 한국 표준시 (UTC+9)
- **계산**: 매주 월요일 자정부터 현재까지

### 4.3 메시지 형식

- **타입**: Ephemeral (본인만 보임)
- **포맷**: Slack mrkdwn
- **내용**: 담당자가 작성한 ProjectUpdate 본문 그대로 (AI 요약 없음)

### 4.4 관련 파일

| 파일 | 역할 |
|------|------|
| `src/handlers/initiative-update.ts` | 메인 핸들러 로직 |
| `src/services/linear-client.ts` | Linear API 쿼리 (getMyLeadInitiatives, getInitiativeProjects, getProjectUpdates) |
| `src/types/index.ts` | TypeScript 타입 정의 (LinearInitiative, LinearProjectUpdate) |
| `src/index.ts` | 라우터 등록 |

---

## 5. 비교: /health-update vs /initiative-update

| 항목 | /health-update | /initiative-update |
|------|----------------|-------------------|
| **대상** | 프로젝트 담당자 | 이니셔티브 리드 |
| **범위** | 본인이 Lead인 프로젝트 | 본인이 Lead인 이니셔티브 → 프로젝트들 |
| **데이터** | 프로젝트 이슈 (Issue) | 프로젝트 업데이트 (ProjectUpdate) |
| **분류** | 만든 결과 / 만들 결과 (이슈 상태 기반) | 이니셔티브별 → 프로젝트별 |
| **AI 요약** | ✅ 사용 (Claude Haiku) | ❌ 사용 안함 (담당자 작성 내용 그대로) |

---

## 6. 다음 단계

### 6.1 사용자 피드백 수집

- [ ] 출력 형식이 읽기 편한가?
- [ ] 필요한 정보가 모두 포함되어 있는가?
- [ ] 업데이트 없는 프로젝트 표시 방식이 적절한가?

### 6.2 개선 아이디어

- 특정 이니셔티브만 조회하는 옵션 추가
- 기간 범위 커스터마이징 (이번 주 / 지난 주 / 이번 달)
- SubInitiatives 지원
- 프로젝트 상태(health) 표시

---

## 부록: 배포 정보

```
Worker Name: slack-linear-sync
URL: https://slack-linear-sync.ny-4f1.workers.dev
Version: ced6aa10-b77d-4c4f-924b-6b7d61f67358
Upload Size: 288.35 KiB (gzip: 61.25 KiB)
Startup Time: 19ms
Deployed: 2026-01-29T08:36:00Z

Commits:
- cd66bce feat(router): register /initiative-update command
- 9e305f4 feat(handlers): add initiative-update handler
- 16e30d6 feat(linear): add initiative queries and types
```
